AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Solución Serverless para AWS Academy: S3 + API Gateway REST + Lambda + DynamoDB.
  Usa el rol LabRole existente en lugar de crear roles IAM propios.
  IMPORTANTE: No crea ni modifica recursos IAM (incompatible con AWS Academy).

# ============================================================
# PARÁMETROS
# ============================================================
Parameters:

  ProjectName:
    Type: String
    Default: mis-vehiculos
    Description: Nombre base del proyecto (prefijo en los recursos)

  StageName:
    Type: String
    Default: v1
    AllowedValues: [v1, v2, prod, dev]
    Description: Nombre del stage de API Gateway

  DynamoDBBillingMode:
    Type: String
    Default: PAY_PER_REQUEST
    AllowedValues: [PAY_PER_REQUEST, PROVISIONED]
    Description: Modo de facturación de DynamoDB

  # AWS Academy proporciona el LabRole con ARN fijo en todas las cuentas de laboratorio.
  # Si tu cuenta tiene un ARN diferente, cámbialo aquí antes de desplegar.
  LabRoleArn:
    Type: String
    Default: arn:aws:iam::ACCOUNT_ID:role/LabRole
    Description: >
      ARN del rol LabRole proporcionado por AWS Academy.
      Sustitúyelo con el ARN real de tu cuenta de laboratorio.
      Lo puedes encontrar en IAM > Roles > LabRole.

# ============================================================
# RECURSOS
# ============================================================
Resources:

  # ----------------------------------------------------------
  # 1. TABLA DYNAMODB
  # ----------------------------------------------------------
  VehiculosTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${ProjectName}-vehiculos"
      BillingMode: !Ref DynamoDBBillingMode
      # Clave primaria compuesta:
      #   - record_type (PK)  → diferencia tipos de registro (vehiculo, localizacion, etc.)
      #   - id           (SK)  → identificador único generado por la Lambda
      AttributeDefinitions:
        - AttributeName: record_type
          AttributeType: S
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: record_type
          KeyType: HASH
        - AttributeName: id
          KeyType: RANGE
      Tags:
        - Key: Project
          Value: !Ref ProjectName

  # ----------------------------------------------------------
  # 2. FUNCIÓN LAMBDA  (usa LabRole directamente, sin crear rol IAM)
  # ----------------------------------------------------------
  VehiculosLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub "${ProjectName}-api"
      Description: "API REST para gestión de vehículos con CORS habilitado (AWS Academy)"
      Runtime: python3.12
      Handler: index.lambda_handler
      # LabRole en lugar de un rol creado por la plantilla
      Role: !Ref LabRoleArn
      Timeout: 30
      MemorySize: 128
      Environment:
        Variables:
          TABLE_NAME: !Ref VehiculosTable
      Code:
        ZipFile: |
          # AWS SDK for Python (Boto3) - https://aws.amazon.com/sdk-for-python/
          # Lambda function: REST API para gestión de vehículos con CORS habilitado.
          import json
          import os
          import boto3
          import uuid
          from datetime import datetime

          dynamodb = boto3.resource('dynamodb')
          table = dynamodb.Table(os.environ['TABLE_NAME'])

          HEADERS = {
              'Access-Control-Allow-Origin': '*',
              'Access-Control-Allow-Methods': 'GET, POST, PUT, OPTIONS',
              'Access-Control-Allow-Headers': 'Content-Type'
          }

          def lambda_handler(event, context):
              # Manejar peticiones OPTIONS (Preflight CORS)
              if event['httpMethod'] == 'OPTIONS':
                  return {
                      'statusCode': 200,
                      'headers': HEADERS,
                      'body': json.dumps('OK')
                  }

              http_method = event['httpMethod']
              path = event['path']

              if path == '/vehiculos':
                  if http_method == 'GET':
                      return listar_vehiculos()
                  elif http_method == 'POST':
                      return crear_vehiculo(json.loads(event['body']) if 'body' in event else {})
                  elif http_method == 'PUT':
                      return actualiza_vehiculo(json.loads(event['body']) if 'body' in event else {})
              elif path == '/':
                  if http_method == 'GET':
                      return {
                          'statusCode': 200,
                          'headers': HEADERS,
                          'body': json.dumps({'message': 'Mis Vehiculos - Página principal'})
                      }

              return {
                  'statusCode': 404,
                  'headers': HEADERS,
                  'body': json.dumps({'error': 'Not Found'})
              }

          def crear_vehiculo(data):
              if not data:
                  return {
                      'statusCode': 400,
                      'headers': HEADERS,
                      'body': json.dumps({'error': 'Missing request body'})
                  }
              item = {
                  'id': str(uuid.uuid4())[:10],
                  'record_type': 'vehiculo',
                  'createdAt': datetime.now().isoformat(),
                  **data
              }
              table.put_item(Item=item)
              return {
                  'statusCode': 201,
                  'headers': HEADERS,
                  'body': json.dumps(item)
              }

          def actualiza_vehiculo(data):
              if not data or 'id' not in data:
                  return {
                      'statusCode': 400,
                      'headers': HEADERS,
                      'body': json.dumps({'error': 'Missing id in request body'})
                  }
              item_id = data['id']
              actualiza_expression = 'SET updatedAt = :updatedAt'
              expression_values = {':updatedAt': datetime.now().isoformat()}
              for key, value in data.items():
                  if key != 'id':
                      actualiza_expression += f', {key} = :{key}'
                      expression_values[f':{key}'] = value
              table.update_item(
                  Key={'id': item_id, 'record_type': 'vehiculo'},
                  UpdateExpression=actualiza_expression,
                  ExpressionAttributeValues=expression_values,
                  ReturnValues='ALL_NEW'
              )
              return {
                  'statusCode': 200,
                  'headers': HEADERS,
                  'body': json.dumps({'id': item_id, 'message': 'vehiculo updated successfully'})
              }

          def listar_vehiculos():
              response = table.query(
                  KeyConditionExpression='record_type = :record_type_val',
                  ExpressionAttributeValues={':record_type_val': 'vehiculo'}
              )
              return {
                  'statusCode': 200,
                  'headers': HEADERS,
                  'body': json.dumps(response.get('Items', []))
              }
      Tags:
        - Key: Project
          Value: !Ref ProjectName

  # ----------------------------------------------------------
  # 3. API GATEWAY REST
  # ----------------------------------------------------------
  VehiculosApi:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: !Sub "${ProjectName}-api"
      Description: "API REST para gestión de vehículos (AWS Academy)"
      EndpointConfiguration:
        Types:
          - REGIONAL
      Tags:
        - Key: Project
          Value: !Ref ProjectName

  # --- Recurso /vehiculos ---
  VehiculosResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      RestApiId: !Ref VehiculosApi
      ParentId: !GetAtt VehiculosApi.RootResourceId
      PathPart: vehiculos

  # --- GET /vehiculos ---
  VehiculosGetMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref VehiculosApi
      ResourceId: !Ref VehiculosResource
      HttpMethod: GET
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub
          - "arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations"
          - LambdaArn: !GetAtt VehiculosLambda.Arn

  # --- POST /vehiculos ---
  VehiculosPostMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref VehiculosApi
      ResourceId: !Ref VehiculosResource
      HttpMethod: POST
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub
          - "arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations"
          - LambdaArn: !GetAtt VehiculosLambda.Arn

  # --- PUT /vehiculos ---
  VehiculosPutMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref VehiculosApi
      ResourceId: !Ref VehiculosResource
      HttpMethod: PUT
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub
          - "arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations"
          - LambdaArn: !GetAtt VehiculosLambda.Arn

  # --- OPTIONS /vehiculos (CORS Preflight) ---
  VehiculosOptionsMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref VehiculosApi
      ResourceId: !Ref VehiculosResource
      HttpMethod: OPTIONS
      AuthorizationType: NONE
      Integration:
        Type: MOCK
        RequestTemplates:
          application/json: '{"statusCode": 200}'
        IntegrationResponses:
          - StatusCode: '200'
            ResponseParameters:
              method.response.header.Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key'"
              method.response.header.Access-Control-Allow-Methods: "'GET,POST,PUT,OPTIONS'"
              method.response.header.Access-Control-Allow-Origin: "'*'"
            ResponseTemplates:
              application/json: ''
      MethodResponses:
        - StatusCode: '200'
          ResponseParameters:
            method.response.header.Access-Control-Allow-Headers: true
            method.response.header.Access-Control-Allow-Methods: true
            method.response.header.Access-Control-Allow-Origin: true

  # --- GET / (raíz) ---
  RootGetMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref VehiculosApi
      ResourceId: !GetAtt VehiculosApi.RootResourceId
      HttpMethod: GET
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub
          - "arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations"
          - LambdaArn: !GetAtt VehiculosLambda.Arn

  # --- OPTIONS / (raíz CORS Preflight) ---
  RootOptionsMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref VehiculosApi
      ResourceId: !GetAtt VehiculosApi.RootResourceId
      HttpMethod: OPTIONS
      AuthorizationType: NONE
      Integration:
        Type: MOCK
        RequestTemplates:
          application/json: '{"statusCode": 200}'
        IntegrationResponses:
          - StatusCode: '200'
            ResponseParameters:
              method.response.header.Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key'"
              method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
              method.response.header.Access-Control-Allow-Origin: "'*'"
            ResponseTemplates:
              application/json: ''
      MethodResponses:
        - StatusCode: '200'
          ResponseParameters:
            method.response.header.Access-Control-Allow-Headers: true
            method.response.header.Access-Control-Allow-Methods: true
            method.response.header.Access-Control-Allow-Origin: true

  # --- Deployment del API ---
  ApiDeployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn:
      - VehiculosGetMethod
      - VehiculosPostMethod
      - VehiculosPutMethod
      - VehiculosOptionsMethod
      - RootGetMethod
      - RootOptionsMethod
    Properties:
      RestApiId: !Ref VehiculosApi
      Description: !Sub "Deployment inicial - ${ProjectName} (Academy)"

  # --- Stage del API ---
  # NOTA: Se omite MethodSettings con LoggingLevel porque en AWS Academy
  # API Gateway no tiene permisos para escribir en CloudWatch Logs
  # sin un rol de cuenta configurado previamente (account-level setting).
  ApiStage:
    Type: AWS::ApiGateway::Stage
    Properties:
      RestApiId: !Ref VehiculosApi
      DeploymentId: !Ref ApiDeployment
      StageName: !Ref StageName
      Description: !Sub "Stage ${StageName} - ${ProjectName} (Academy)"
      Tags:
        - Key: Project
          Value: !Ref ProjectName

  # --- Permiso para que API Gateway invoque la Lambda ---
  LambdaApiGatewayPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt VehiculosLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${VehiculosApi}/*/*/*"

  # ----------------------------------------------------------
  # 4. BUCKET S3 PARA HOSTING WEB ESTÁTICO
  # ----------------------------------------------------------
  WebsiteBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "${ProjectName}-web-${AWS::AccountId}"
      WebsiteConfiguration:
        IndexDocument: indexTabla.html
        ErrorDocument: index.html
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false
      CorsConfiguration:
        CorsRules:
          - AllowedHeaders:
              - "*"
            AllowedMethods:
              - GET
              - HEAD
            AllowedOrigins:
              - "*"
            MaxAge: 3000
      Tags:
        - Key: Project
          Value: !Ref ProjectName

  # --- Política del bucket S3 para acceso público de lectura ---
  WebsiteBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref WebsiteBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: PublicReadGetObject
            Effect: Allow
            Principal: "*"
            Action: s3:GetObject
            Resource: !Sub "${WebsiteBucket.Arn}/*"

  # ----------------------------------------------------------
  # 5. CLOUDWATCH LOG GROUP PARA LA LAMBDA
  # ----------------------------------------------------------
  LambdaLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${ProjectName}-api"
      RetentionInDays: 7

# ============================================================
# OUTPUTS
# ============================================================
Outputs:

  ApiUrl:
    Description: "URL base de la API REST"
    Value: !Sub "https://${VehiculosApi}.execute-api.${AWS::Region}.amazonaws.com/${StageName}"

  ApiVehiculosUrl:
    Description: "Endpoint /vehiculos de la API REST"
    Value: !Sub "https://${VehiculosApi}.execute-api.${AWS::Region}.amazonaws.com/${StageName}/vehiculos"

  DynamoDBTableName:
    Description: "Nombre de la tabla DynamoDB"
    Value: !Ref VehiculosTable

  LambdaFunctionName:
    Description: "Nombre de la función Lambda"
    Value: !Ref VehiculosLambda

  WebsiteBucketName:
    Description: "Nombre del bucket S3"
    Value: !Ref WebsiteBucket

  WebsiteUrl:
    Description: "URL del sitio web estático en S3"
    Value: !GetAtt WebsiteBucket.WebsiteURL

  WebsiteUrlTabla:
    Description: "URL directa a la página de tabla de vehículos"
    Value: !Sub "${WebsiteBucket.WebsiteURL}/indexTabla.html"

  LabRoleUsed:
    Description: "ARN del LabRole utilizado por la Lambda"
    Value: !Ref LabRoleArn
